# ⚠️ CRITICAL: DO NOT COMMIT .env TO GIT

# Environment Configuration Template
# Copy this file to .env and fill in your actual values

# Server Configuration
PORT=3000
NODE_ENV=production

# Database Configuration (Hostinger MySQL)
# IMPORTANT: Update these with your production credentials
DB_HOST=srv2054.hstgr.io
DB_USER=u120899366_zefeender
DB_PASS=INSERT_YOUR_DB_PASSWORD_HERE
DB_NAME=u120899366_zefender
DB_DIALECT=mysql

# Webhook Secret (for ESP32 authentication)
# Generate a strong secret for production
RAZORPAY_WEBHOOK_SECRET=INSERT_STRONG_SECRET_HERE

# Session Secret
# CRITICAL: Generate a strong random string (min 32 characters)
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=INSERT_STRONG_SESSION_SECRET_HERE

# Admin Credentials (for initial setup)
ADMIN_USER=admin
ADMIN_PASS=INSERT_STRONG_ADMIN_PASSWORD_HERE

# Base URL
# PRODUCTION: Use your actual domain with HTTPS
# DEVELOPMENT: http://localhost:3000
BASE_URL=https://admin.zefender.com

# Timezone
TZ=Asia/Kolkata

# Optional: Error Tracking (Sentry, etc.)
# SENTRY_DSN=your_sentry_dsn_here

# Optional: Email Configuration (for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password
